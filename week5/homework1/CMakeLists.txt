cmake_minimum_required(VERSION 2.8)
project( homework1 )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( downscale.o downscale.cpp )
target_link_libraries( downscale.o ${OpenCV_LIBS} )